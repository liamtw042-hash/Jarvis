# ============================================================
# JARVIS - Voice Assistant Requirements
# Python 3.11+ compatible (including 3.14)
# Install with: pip install -r requirements.txt
#
# All packages here use pre-built binary wheels on Windows —
# no Microsoft C++ Build Tools required.
# ============================================================

# --- AI & Language Models ---
anthropic>=0.25.0          # Claude AI (brain of JARVIS)
openai>=1.30.0             # Whisper STT transcription

# --- Voice Input ---
SpeechRecognition>=3.10.0  # Google STT for wake-word (pure Python — no PyAudio needed)
sounddevice>=0.4.6         # Microphone recording (pre-built wheel, bundles PortAudio)

# --- Voice Output ---
elevenlabs>=1.2.0          # British voice TTS
pyttsx3>=2.90              # Fallback TTS (uses Windows SAPI5 — no compilation)
sounddevice>=0.4.6         # Audio playback (same package as above)
soundfile>=0.12.1          # Read/write WAV in-memory (pre-built wheel, bundles libsndfile)

# --- Web & Data ---
requests>=2.31.0           # HTTP requests
python-dotenv>=1.0.0       # Load .env API keys
beautifulsoup4>=4.12.3     # HTML parsing for web results
duckduckgo-search>=6.1.0   # Web search (no API key needed)
yfinance>=0.2.36           # Forex OHLC data for market scanner (pre-built wheel)
pandas>=2.1.0              # DataFrame resampling for 4H candles (pre-built wheel)

# --- Math & Audio ---
numpy>=1.26.0              # Activation sound synthesis & audio processing

# --- Windows System Control ---
pycaw>=20181226            # Windows audio volume control (pure Python COM)
comtypes>=1.4.0            # COM interface (pure Python, required by pycaw)

# --- Optional: Local Whisper (offline STT without OpenAI API key) ---
# Requires ~1.5GB disk space and ffmpeg. Uncomment to enable.
# openai-whisper>=20231117
# torch>=2.1.0
